# Copy this file to .env and replace every placeholder locally. Never commit .env.
DATABASE_URL=postgresql+psycopg2://YOUR_DATABASE_USER:YOUR_DATABASE_PASSWORD@localhost/chatbot
SECRET_KEY=replace-with-a-long-random-value
# Set this to true only when the app is served over HTTPS.
SESSION_COOKIE_SECURE=false
